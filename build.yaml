# This file generates the GitHub Actions matrix
# For simple board + shield combinations, add them
# to the top level board and shield arrays, for more
# control, add individual board + shield combinations to
# the `include` property, e.g:
#
# board: [ "nice_nano_v2" ]
# shield: [ "corne_left", "corne_right" ]
# include:
#   - board: bdn9_rev2
#   - board: nice_nano_v2
#     shield: reviung41
#
---
include:
  - name: corne
    board: nice_nano_v2
    shield: settings_reset
    artifact-name: corne_settings_reset

  - name: typeractive_corne
    board: nice_nano_v2
    shield: corne_left nice_view_adapter nice_view
    snippet: studio-rpc-usb-uart
    artifact-name: typeractive_corne_left
  - name: typeractive_corne
    board: nice_nano_v2
    shield: corne_right nice_view_adapter nice_view
    artifact-name: typeractive_corne_right

  - name: panda_corne
    board: nice_nano_v2
    shield: corne_left nice_oled
    # cmake-args: "-DZMK_CONFIG_OVERLAY_FILE=${ZEPHYR_BASE}/../config/panda.conf"
    artifact-name: panda_corne_left
  - name: panda_corne
    board: nice_nano_v2
    shield: corne_right nice_oled
    # cmake-args: "-DZMK_CONFIG_OVERLAY_FILE=${ZEPHYR_BASE}/../config/panda.conf"
    artifact-name: panda_corne_right
